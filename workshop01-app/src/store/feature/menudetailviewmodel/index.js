import { createSlice } from "@reduxjs/toolkit";

export const menuDetailViewModelSlice = createSlice({
  name: "menudetailviewmodel",
  initialState: {},
  reducers: {},
});
export const {} = menuDetailViewModelSlice.actions;
export default menuDetailViewModelSlice.reducer;
